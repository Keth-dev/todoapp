name: 08-03-Contexts - Inputs

on:
  workflow_dispatch:
   
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Use Workflow Input
        run: |
          echo "DEBUG: ${{ vars.debug }}"
